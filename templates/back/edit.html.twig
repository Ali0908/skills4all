{% extends 'base.html.twig' %}

{% block title %}Edit Car{% endblock %}

{% block body %}
    <h1>Edit Car</h1>

    {{ include('back/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_back_index') }}">back to list</a>

    {{ include('back/_delete_form.html.twig') }}
{% endblock %}
