{% extends 'base.html.twig' %}

{% block title %}Car{% endblock %}

{% block body %}
    <h1>Car</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ car.name }}</td>
            </tr>
            <tr>
                <th>NbreSeats</th>
                <td>{{ car.nbreSeats }}</td>
            </tr>
            <tr>
                <th>NbreDoors</th>
                <td>{{ car.nbreDoors }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ car.cost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_index') }}">back to list</a>

    <a href="{{ path('app_back_edit', {'id': car.id}) }}">edit</a>

    {{ include('back/_delete_form.html.twig') }}
{% endblock %}
